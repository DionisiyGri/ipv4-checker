package ipchecker

type Result struct {
	Lines  uint64
	Unique uint64
}

func Execute(path string) (Result, error) {
	//read file at the provided path
	//parse IPs from file
	//logic to check and unique count

	return Result{}, nil
}
